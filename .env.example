# Supabase — public (safe to expose to the browser / Next.js client)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-only (NEVER expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=
